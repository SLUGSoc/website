<%= bootstrap_form_with(model: server, local: true) do |form| %>
  <% if server.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(server.errors.count, "error") %> prohibited this server from being saved:</h2>

      <ul>
      <% server.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.collection_select :game_id, Game.order(:name),:id,:name, include_blank: false, custom: true %>
  </div>

  <div class="field">
    <%= form.number_field :port %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

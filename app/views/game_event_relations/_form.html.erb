<%= form_with(model: game_event_relation, local: true) do |form| %>
  <% if game_event_relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_event_relation.errors.count, "error") %> prohibited this game_event_relation from being saved:</h2>

      <ul>
      <% game_event_relation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.text_field :event_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

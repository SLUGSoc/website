<%= bootstrap_form_with(model: rule, local: true) do |form| %>
  <% if rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rule.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
      <% rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.collection_select :code_id, Code.order(:name),:id,:name, include_blank: false %>
  </div>

  <div class="field">
    <%= form.text_field :summary %>
  </div>

  <div class="field">
    <%= form.text_area :full %>
  </div>

  <div class="field">
    <%= form.text_field :icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
